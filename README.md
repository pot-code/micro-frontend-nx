# MicroFrontendNx

微前端测试库，将 vue、react 集成进 react 项目里

## 运行

```bash
# shell 1
npx nx run-many -t preview -p vue-remote react-remote

# shell 2
npx nx serve react-shell
```

浏览器打开 http://localhost:4200
